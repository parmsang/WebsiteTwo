  <% if charity_signed_in? %>
  <% else %>
  <div id="help_link"><%= link_to "Request help for your charity", new_charity_registration_path, class: "button" %></div>
<% end %>
<div class="showcase_project">Agile Ventures developers provide their services for free in order to further develop their IT skills and contribute to the greater good. We have already helped many charities and we can help your charity too. Just tell us your idea and our developers will review it and get in touch.
<h1>Charities we have helped</h1></div>

<% if @projects.any? %>
  <% @projects.each do |project| %>
    <%= render partial: 'partials/project', locals: {project: project} %>
  <% end %>
<% else %>
  <h1>No projects in showcase!</h1>
<% end %>

<a href='projects/new' class="button">Add a project</a>

<% if charity_signed_in? %>
  <% else %>
  <div id="help_link"><%= link_to "How can we help you?", new_charity_registration_path, class: "button" %></div>
<% end %>
